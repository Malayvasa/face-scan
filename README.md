# Face scan

A live-camera website that measures 32 facial features in millimeters.

Clone this repo, then run:

```bash
pnpm install
pnpm dev
```

Open http://localhost:3000, allow the camera, hold a credit card next to your face, tap both ends, then scan left, front, and right.
